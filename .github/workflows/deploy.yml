name: Deploy to biks-droplet

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy SBSR WhatsApp Bridge

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -t ed25519 206.189.34.228 >> ~/.ssh/known_hosts 2>/dev/null
          # Fallback to RSA
          ssh-keyscan -t rsa 206.189.34.228 >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy via SSH
        run: |
          ssh -o StrictHostKeyChecking=accept-new root@206.189.34.228 << 'EOF'
            set -e
            echo "🚀 Deploying wa-bridge-sbsr..."

            cd /docker/wa-webhook-sbsr

            # Stash local changes (backup files, etc)
            git stash --include-untracked 2>/dev/null || true

            # Pull latest
            echo "📥 Pulling latest from GitHub..."
            git pull origin master

            # Install deps if package.json changed
            echo "📦 Checking dependencies..."
            npm install --production 2>/dev/null || true

            # Validate syntax
            echo "🔍 Checking syntax..."
            node --check server.js
            node --check admin.js

            # Restart PM2
            echo "🔄 Restarting PM2 process..."
            pm2 restart wa-bridge-sbsr --update-env

            echo "✅ Deploy complete!"
          EOF

      - name: Health check
        run: |
          sleep 5
          curl -sf https://production.biks.ai/admin/api/stats | head -3
          echo ""
          echo "✅ Health check passed"
